apiVersion: v1
kind: Namespace
metadata:
  name: ReactTodoUI-ns
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ReactTodoUI-app-deployment
  namespace: ReactTodoUI-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ReactTodoUI-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ReactTodoUI-app
    spec:
      containers:
        - name: ReactTodoUI-app
          image: aksclsuterregistry.azurecr.io/reacttodoui:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ReactTodoUI-ingress
  namespace: ReactTodoUI-ns
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: ReactTodoUI-app-service
              port:
                number: 80

---
apiVersion: v1
kind: Service
metadata:
  name: ReactTodoUI-app-service
  namespace: ReactTodoUI-ns
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ReactTodoUI-app
  ports:
    - port: 80
      targetPort: 80
